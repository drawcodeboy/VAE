expr: Experiment

device: cuda:2

data:
  dataset: CelebA
  mode: train # should consider train or test!

model:
  name: VAE
  dims: [3, 32, 64, 128]
  latent: 100
  img_size: [3, 128, 128]

loss_fn: 
  name: ELBO
 
hyperparams:
  optim: AdamW
  lr: 0.0001
  weight_decay: 0.001
  epochs: 200
  batch_size: 128

save_path: saved